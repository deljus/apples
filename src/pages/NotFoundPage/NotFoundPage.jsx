import React from 'react';
import { PageHeader, PageBody, PageContent } from '../../components';

const NotFoundPage = () => (
  <PageContent>
    <PageHeader>Not Found</PageHeader>
    <PageBody>Страница не найдена</PageBody>
  </PageContent>
);

export default NotFoundPage;
